Django==6.0.7
djangorestframework==3.17.1
djangorestframework-simplejwt==5.5.1
django-cors-headers==4.9.0
django-filter==26.1
psycopg2-binary==2.9.12
dj-database-url==2.3.0
python-decouple==3.8
requests==2.32.3
channels==4.2.0
# channels_redis==4.3.0  -- uncomment + install once REDIS_URL is set on
# Railway (needed only when running more than one backend process; see
# settings.py's CHANNEL_LAYERS comment). Not needed for a single process.
gunicorn==23.0.0
daphne==4.1.2
